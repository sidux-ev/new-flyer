how to ...
we need ascii - file
we should use odt to get idea how many text a DIN A6 (postcard) can carry
and pdf is never wrong ;)
so try that way:
clone / pull
open the odt and make your changes
than save it (in odt dir)
than save as text (in txt dir) 
than export as pdf (in pdf dir)

